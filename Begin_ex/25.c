#include <stdio.h>
#include <math.h>

int main(){
    while(1){
int X, Y;
printf(" Enter  the X term: ");
scanf("%d", &X);

Y=3*pow(X,6)-6*pow(X,2)-7;
printf("Y_function teng: %d\n", Y);
    }
    return 0;
}
