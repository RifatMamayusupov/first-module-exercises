#include <stdio.h>
#include <math.h>

int main(){
    while(1){
int a;
int V,S;
printf(" Enter  the A term: ");
scanf("%d", &a);

V=pow(a,3);
printf("Kubning_hajmi: %d\n", V);
S=6*a*a;
printf("Kubning_to'la-sirti: %d\n", S);
    }
    return 0;
}