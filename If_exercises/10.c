#include <stdio.h>
#include <stdlib.h>
int main (){
    while (1){
int A, B,sum;
  printf("Enter the A term :");
  scanf ("%d",&A);
  printf("Enter the B term :");
  scanf ("%d",&B);
  if(A!=B) printf("A va B_ning qiymatI: %d\n",sum=A+B);
  else printf("A va B_teng:%d\n",0);
    }
    return 0;
}