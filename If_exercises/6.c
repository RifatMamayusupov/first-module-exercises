#include <stdio.h>
#include <stdlib.h>
int main () {
    while (1){
int son, son1;
printf("Enter the Son term :");
scanf("%d",&son);
printf("Enter the Son1 term :");
scanf("%d", &son1);
if(son>son1) printf("Son va Son1 oraidagi katta_son:%d\n",son);
else printf("Son va Son1 orasidagi katta son: %d\n",son1);

    }
    return 0;
}